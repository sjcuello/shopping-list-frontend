export interface Card { 
  id: number;
  name: string;
  description: string;
  amount: number;
  isChecked: boolean;
}

export interface ItemForm { 
  name: string;
  description: string;
  amount: number;
}
